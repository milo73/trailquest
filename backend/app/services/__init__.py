"""Backend service modules.

The MVP is a modular monolith: each module here corresponds to a service in the
target architecture (PRD §9.1) and keeps a clean boundary so it can be split out
into a standalone stateless service later.
"""
